[project]
name = "ado-dashboard"
version = "0.1.0"
description = "Terminal dashboard for Azure DevOps sprint work items"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.47",
    "azure-devops>=7.1",
    "msrest>=0.7",
]

[project.scripts]
ado-dashboard = "dashboard:main"
